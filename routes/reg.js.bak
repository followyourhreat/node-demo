var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/reg', function(req, res, next) {
  res.render('reg', { title: '用户注册2222' });
});

module.exports = router;
